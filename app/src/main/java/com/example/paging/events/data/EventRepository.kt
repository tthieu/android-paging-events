package com.example.paging.events.data

class EventRepository(
//    private val service: GithubService,
//    private val database:
) {
    // Viet function de convert response thanh database object trong app
    // Viet function de convert database object thanh data trong app (domain)
    // can con: datasource, remote mediator, repository
}